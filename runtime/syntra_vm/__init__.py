"""
Placeholder package for a future SyntraLine++ virtual machine (VM) runtime.

In the future, this could implement a higher-level IR interpreter that
runs entire experiments directly from the IR, without generating Python
scripts per experiment.
"""